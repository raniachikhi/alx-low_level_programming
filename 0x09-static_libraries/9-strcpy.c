#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>
/**
  * _strcpy - it is a function
  * @dest: character
  * @src : character
  * Return: returns a char
  */

char *_strcpy(char *dest, char *src)
{
	return (strcpy(dest, src));
}
