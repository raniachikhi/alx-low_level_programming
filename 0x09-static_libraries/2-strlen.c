#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

/**
  * _strlen - it is a function
  * @s : character
  * Return: returns strlen(s)
  */

int _strlen(char *s)
{
	return (strlen(s));
}
