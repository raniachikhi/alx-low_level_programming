#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

/**
  * _isalpha- it is a function
  * @c : int
  * Return: it returns isalpha(c)
  */

int _isalpha(int c)
{
	return (isalpha(c));
}
