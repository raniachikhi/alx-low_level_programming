#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

/**
  * _abs- it's a function
  * @n : int
  * Return: abs(n)
  */

int _abs(int n)
{
	return (abs(n));
}
